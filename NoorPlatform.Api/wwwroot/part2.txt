           
        .attendance-header > div:first-child {
            flex: 1 1 280px;
            min-width: 0;
        }

        .date-nav {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: flex-end;
            gap: 10px;
            background: #fff;
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 10px 14px;
            box-shadow: var(--shadow);
            flex: 0 1 auto;
            max-width: 100%;
            box-sizing: border-box;
        }

        .btn-bulk-absence {
            font-size: 11px;
            line-height: 1.35;
            padding: 6px 10px !important;
            white-space: normal;
            max-width: 150px;
            flex-shrink: 1;
        }

        .date-nav-dates {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-shrink: 0;
        }

        .date-nav button {
            width: 32px;
            height: 32px;
            border-radius: 8px;
            border: 1px solid var(--border);
            background: var(--bg);
            cursor: pointer;
            font-size: 14px;
            transition: .2s;
        }

        .date-nav button:hover {
            border-color: var(--green);
            color: var(--green);
        }

        .date-nav span {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            min-width: 140px;
            text-align: center;
        }

        .circle-selector {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        .circle-chip {
            padding: 8px 18px;
            border-radius: 20px;
            border: 2px solid var(--border);
            background: #fff;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            transition: .2s;
            font-family: 'Tajawal', sans-serif;
        }

        .circle-chip.active {
            background: var(--gradient);
            color: #fff;
            border-color: transparent;
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }

        .circle-chip:hover:not(.active) {
            border-color: var(--green);
            color: var(--green);
        }

        .attendance-table-wrap {
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            overflow: hidden;
        }

        .att-row {
            display: flex;
            align-items: center;
            padding: 14px 22px;
            border-bottom: 1px solid rgba(226, 232, 240, 0.5);
            transition: background .15s;
            gap: 16px;
        }

        .att-row:last-child {
            border-bottom: none;
        }

        .att-row:hover {
            background: var(--bg);
        }

        .att-num {
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: var(--bg);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 700;
            color: var(--text-muted);
            flex-shrink: 0;
        }

        .att-name {
            flex: 1;
            font-size: 14px;
            font-weight: 600;
        }

        .att-phone {
            font-size: 12px;
            color: var(--text-muted);
        }

        .att-btns {
            display: flex;
            gap: 6px;
        }

        .att-btn {
            width: 38px;
            height: 38px;
            border-radius: 10px;
            border: 2px solid var(--border);
            background: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: .2s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .att-btn:hover {
            transform: scale(1.1);
        }

        .att-btn.present {
            border-color: var(--green);
            background: var(--green-light);
        }

        .att-btn.absent {
            border-color: #ef4444;
            background: #fee2e2;
        }

        .att-btn.late {
            border-color: #f59e0b;
            background: #fef3c7;
        }

        /* ===== MEMORIZATION PAGE ===== */
        .memorize-layout {
            display: grid;
            grid-template-columns: 1fr 340px;
            gap: 20px;
        }

        .surah-progress-grid {
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            overflow: hidden;
        }

        .surah-item {
            display: flex;
            align-items: center;
            padding: 14px 22px;
            border-bottom: 1px solid rgba(226, 232, 240, 0.5);
            gap: 14px;
            transition: background .15s;
        }

        .surah-item:last-child {
            border-bottom: none;
        }

        .surah-item:hover {
            background: var(--bg);
        }

        .surah-num {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: var(--gradient);
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 700;
            flex-shrink: 0;
        }

        .surah-info {
            flex: 1;
        }

        .surah-info h4 {
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 6px;
        }

        .surah-status {
            font-size: 11px;
            font-weight: 700;
            padding: 3px 10px;
            border-radius: 20px;
        }

        .surah-done {
            background: var(--green-light);
            color: var(--green-dark);
        }

        .surah-progress-badge {
            background: var(--blue-light);
            color: var(--blue-dark);
        }

        .surah-pending {
            background: var(--bg);
            color: var(--text-muted);
            border: 1px solid var(--border);
        }

        /* Sidebar panel */
        .side-panel {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .panel-card {
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            overflow: hidden;
        }

        .panel-card-header {
            padding: 16px 20px;
            border-bottom: 1px solid var(--border);
            font-size: 14px;
            font-weight: 800;
        }

        .panel-card-body {
            padding: 16px 20px;
        }

        /* ===== EXAMS PAGE ===== */
        .exam-cards {
            display: grid;
            gap: 16px;
        }

        .exam-card {
            background: var(--card);
            border-radius: var(--radius);
            padding: 20px 22px;
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            display: flex;
            align-items: center;
            gap: 18px;
            transition: transform .25s;
        }

        .exam-card:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .exam-icon {
            width: 54px;
            height: 54px;
            border-radius: 14px;
            background: var(--gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 22px;
            flex-shrink: 0;
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }

        .exam-info {
            flex: 1;
        }

        .exam-info h4 {
            font-size: 15px;
            font-weight: 800;
            margin-bottom: 4px;
        }

        .exam-info p {
            font-size: 12px;
            color: var(--text-muted);
        }

        .exam-meta {
            display: flex;
            gap: 10px;
            margin-top: 8px;
        }

        .exam-tag {
            font-size: 11px;
            font-weight: 600;
            padding: 3px 10px;
            border-radius: 20px;
        }

        .tag-green {
            background: var(--green-light);
            color: var(--green-dark);
        }

        .tag-blue {
            background: var(--blue-light);
            color: var(--blue-dark);
        }

        .tag-orange {
            background: #fef3c7;
            color: #d97706;
        }

        .exam-score {
            text-align: center;
        }

        .exam-score .score-val {
            font-size: 26px;
            font-weight: 900;
            color: var(--green-dark);
        }

        .exam-score .score-max {
            font-size: 12px;
            color: var(--text-muted);
        }

        /* ===== ANNOUNCEMENTS ===== */
        .ann-list {
            display: grid;
            gap: 14px;
        }

        .ann-card {
            background: var(--card);
            border-radius: var(--radius);
            padding: 18px 20px;
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            display: flex;
            gap: 16px;
            transition: transform .25s;
            cursor: pointer;
        }

        .ann-card:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .ann-indicator {
            width: 4px;
            border-radius: 4px;
            flex-shrink: 0;
            align-self: stretch;
        }

        .ann-card-content {
            flex: 1;
        }

        .ann-card-top {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 6px;
        }

        .ann-card-top h4 {
            font-size: 14px;
            font-weight: 800;
            flex: 1;
        }

        .ann-card-top time {
            font-size: 11px;
            color: var(--text-muted);
        }

        .ann-card-content p {
            font-size: 13px;
            color: var(--text-muted);
            line-height: 1.6;
        }

        .ann-target {
            font-size: 11px;
            font-weight: 700;
            margin-top: 8px;
        }

        /* ===== MODAL ===== */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            z-index: 200;
            display: none;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .modal-overlay.open {
            display: flex;
        }

        .modal {
            background: var(--card);
            border-radius: 20px;
            width: 100%;
            max-width: 520px;
            max-height: 85vh;
            overflow-y: auto;
            box-shadow: 0 24px 80px rgba(0, 0, 0, 0.2);
            animation: modalIn .3s cubic-bezier(.4, 0, .2, 1);
        }

        @keyframes modalIn {
            from {
                opacity: 0;
                transform: translateY(30px) scale(0.96);
            }

            to {
                opacity: 1;
                transform: none;
            }
        }

        .modal-header {
            padding: 22px 24px 18px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .modal-header h3 {
            font-size: 18px;
            font-weight: 800;
        }

        .modal-close {
            width: 36px;
            height: 36px;
            border-radius: 10px;
            border: 1px solid var(--border);
            background: var(--bg);
            font-size: 18px;
            cursor: pointer;
            transition: .2s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-close:hover {
            background: #fee2e2;
            color: #dc2626;
            border-color: #dc2626;
        }

        .modal-body {
            padding: 22px 24px;
        }

        .modal-footer {
            padding: 16px 24px;
            border-top: 1px solid var(--border);
            display: flex;
            gap: 10px;
            justify-content: flex-end;
        }

        .form-group {
            margin-bottom: 16px;
        }

        .form-label {
            font-size: 13px;
            font-weight: 700;
            color: var(--text);
            margin-bottom: 6px;
            display: block;
        }

        .form-input {
            width: 100%;
            padding: 11px 14px;
            border-radius: 10px;
            border: 1px solid var(--border);
            font-family: 'Tajawal', sans-serif;
            font-size: 14px;
            color: var(--text);
            background: #fff;
            outline: none;
            transition: .2s;
        }

        .form-input:focus {
            border-color: var(--green);
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }

        /* ===== ROLES PANEL ===== */
        .role-tabs {
            display: flex;
            gap: 8px;
            margin-bottom: 24px;
            background: var(--card);
            border-radius: 14px;
            padding: 6px;
            border: 1px solid var(--border);
            width: fit-content;
            box-shadow: var(--shadow);
        }

        .role-tab {
            padding: 10px 20px;
            border-radius: 10px;
            border: none;
            background: none;
            font-family: 'Tajawal', sans-serif;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: .25s;
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .role-tab.active {
            background: var(--gradient);
            color: #fff;
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }

        .role-tab:not(.active):hover {
            background: var(--bg);
            color: var(--text);
        }

        /* ===== STUDENT VIEW (for student/parent) ===== */
        .student-profile-hero {
            background: var(--gradient);
            border-radius: var(--radius);
            padding: 30px;
            color: #fff;
            display: flex;
            align-items: center;
            gap: 24px;
            margin-bottom: 24px;
            position: relative;
            overflow: hidden;
            box-shadow: var(--shadow-lg);
        }

        .student-profile-hero::before {
            content: '📖';
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 100px;
            opacity: 0.08;
        }

        .hero-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            font-weight: 800;
            border: 3px solid rgba(255, 255, 255, 0.4);
            flex-shrink: 0;
        }

        .hero-info h2 {
            font-size: 22px;
            font-weight: 800;
            margin-bottom: 4px;
        }

        .hero-info p {
            font-size: 13px;
            opacity: 0.8;
        }

        .hero-badges {
            display: flex;
            gap: 8px;
            margin-top: 12px;
        }

        .hero-badge {
            background: rgba(255, 255, 255, 0.2);
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 700;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .student-stats-row {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 14px;
            margin-bottom: 24px;
        }

        /* Toast */
        .toast-container {
            position: fixed;
            bottom: 24px;
            left: 24px;
            z-index: 300;
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .toast {
            background: var(--sidebar-bg);
            color: #fff;
            padding: 12px 20px;
            border-radius: 12px;
            font-size: 13px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
            animation: toastIn .3s, toastOut .3s 2.7s forwards;
            border-right: 4px solid var(--green);
        }

        @keyframes toastIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
        }

        @keyframes toastOut {
            to {
                opacity: 0;
                transform: translateY(20px);
            }
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 1100px) {
            .stat-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .charts-row {
                grid-template-columns: 1fr;
            }

            .memorize-layout {
                grid-template-columns: 1fr;
            }

            .student-stats-row {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            :root {
                --sidebar-w: 260px;
            }

            .sidebar {
                transform: translateX(100%);
            }

            .sidebar.open {
                transform: translateX(0);
            }

            .main {
                margin-right: 0;
            }

            .menu-toggle {
                display: flex;
            }

            .search-box {
                display: none;
            }

            .stat-grid {
                grid-template-columns: 1fr 1fr;
            }

            .content {
                padding: 16px;
            }

            .form-row {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 480px) {
            .stat-grid {
                grid-template-columns: 1fr;
            }

            .student-stats-row {
                grid-template-columns: 1fr 1fr;
            }

            .role-tabs {
                flex-wrap: wrap;
            }
        }

        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--border);
            border-radius: 3px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--text-muted);
        }

        /* Overlay for mobile */
        .sidebar-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 99;
            display: none;
        }

        .sidebar-overlay.open {
            display: block;
        }

        /* ══════════════════════════════════════════════════
           🎨 UI OVERRIDES — تحسينات الهوية البصرية
           مستوحاة من الصور المرجعية للمشروع
        ══════════════════════════════════════════════════ */

        /* ── بطاقات الإحصائيات: Flex layout + خط ملوّن أسفل ── */
        .stat-card {
            display: flex;
            align-items: flex-start;
            gap: 14px;
            padding: 20px;
        }

        .stat-card::after {
            content: '';
            position: absolute;
            bottom: 0;
            right: 0;
            left: 0;
            height: 3px;
            border-radius: 0 0 var(--radius) var(--radius);
        }

        .stat-card.green::after {
            background: #10b981;
        }

        .stat-card.blue::after {
            background: #3b82f6;
        }

        .stat-card.teal::after {
            background: #8b5cf6;
        }

        .stat-card.purple::after {
            background: #f59e0b;
        }

        .stat-card-body {
            flex: 1;
        }

        .stat-value {
            font-size: 28px;
        }

        .stat-icon {
            margin-bottom: 0;
        }

        /* ── بطاقات الطلاب: حواف أنيقة وظل خفيف ── */
        .student-card {
            border-radius: 20px;
            box-shadow: 0 2px 16px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(226, 232, 240, 0.8);
        }

        .student-card:hover {
            box-shadow: 0 8px 32px rgba(16, 185, 129, 0.15);
            border-color: rgba(16, 185, 129, 0.2);
        }

        /* ── شريط البحث في صفحة الطلاب ── */
        .students-search-bar {
            background: var(--card);
            border-radius: 16px;
            padding: 14px 18px;
            margin-bottom: 16px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border);
        }

        .students-search-input {
            width: 100%;
            border: none;
            background: transparent;
            font-family: 'Tajawal', sans-serif;
            font-size: 14px;
            color: var(--text);
            outline: none;
            direction: rtl;
        }

        .students-search-input::placeholder {
            color: var(--text-muted);
        }

        .search-row {
            display: flex;
            align-items: center;
            gap: 10px;
            border-bottom: 1px solid var(--border);
            padding-bottom: 12px;
            margin-bottom: 12px;
        }

        .search-row svg {
            flex-shrink: 0;
            opacity: 0.4;
        }

        /* ── Filter Chips (مثل الصورة: "مستجد", "ذكر", "أسامة") ── */
        .filter-chips {
            display: flex;
            gap: 8px;
            flex-wrap: nowrap;
            overflow-x: auto;
            scrollbar-width: none;
            padding-bottom: 2px;
        }

        .filter-chips::-webkit-scrollbar {
            display: none;
        }

        .filter-chip {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 8px 14px;
            border-radius: 12px;
            border: 1.5px solid var(--border);
            background: var(--card);
            font-family: 'Tajawal', sans-serif;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            white-space: nowrap;
            transition: all .2s;
            color: var(--text);
        }

        .filter-chip:hover {
            border-color: var(--green);
            color: var(--green);
        }

        .filter-chip.active {
            background: var(--green);
            color: #fff;
            border-color: var(--green);
        }

        .filter-chip select {
            border: none;
            background: none;
            font-family: 'Tajawal', sans-serif;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            outline: none;
            color: inherit;
            appearance: none;
            -webkit-appearance: none;
        }

        /* ── بطاقات الطالب: أزرار عرض/تعديل/حذف بالألوان ── */
        .btn-view {
            background: #eff6ff;
            color: #2563eb;
            border: none;
        }

        .btn-edit {
            background: #fefce8;
            color: #ca8a04;
            border: none;
        }

        .btn-delete {
            background: #fef2f2;
            color: #dc2626;
            border: none;
        }

        .btn-view:hover {
            background: #2563eb;
            color: #fff;
        }

        .btn-edit:hover {
            background: #ca8a04;
            color: #fff;
        }

        .btn-delete:hover {
            background: #dc2626;
            color: #fff;
        }

        /* ── Topbar: تاريخ + أيقونات أنيقة ── */
        .topbar-date {
            font-size: 12px;
            color: var(--text-muted);
            font-weight: 600;
            background: var(--bg);
            padding: 5px 12px;
            border-radius: 20px;
            border: 1px solid var(--border);
            white-space: nowrap;
        }

        /* ── FAB (Floating Action Button) — مثل الصورة 3 ── */
        .fab-container {
            position: fixed;
            bottom: 90px;
            left: 20px;
            z-index: 150;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
        }

        .fab-menu {
            display: flex;
            flex-direction: column;
            gap: 8px;
            align-items: flex-start;
            transition: all .3s cubic-bezier(.4, 0, .2, 1);
            pointer-events: none;
            opacity: 0;
            transform: translateY(12px) scale(0.96);
        }

        .fab-menu.open {
            pointer-events: all;
            opacity: 1;
            transform: translateY(0) scale(1);
        }

        .fab-menu-item {
            display: flex;
            align-items: center;
            gap: 10px;
            background: var(--card);
            border-radius: 14px;
            padding: 10px 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.12);
            border: 1px solid var(--border);
            cursor: pointer;
            font-family: 'Tajawal', sans-serif;
            font-size: 13px;
            font-weight: 700;
            color: var(--text);
            white-space: nowrap;
            transition: all .2s;
            -webkit-tap-highlight-color: transparent;
        }

        .fab-menu-item:hover {
            background: var(--green);
            color: #fff;
            border-color: var(--green);
        }

        .fab-menu-item .fmi-icon {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: var(--green-light);
            color: var(--green-dark);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            flex-shrink: 0;
            transition: .2s;
        }

        .fab-menu-item:hover .fmi-icon {
            background: rgba(255, 255, 255, 0.2);
            color: #fff;
        }

        .fab-btn {
